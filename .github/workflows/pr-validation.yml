name: PR validation

on:
  pull_request:
    branches: [main, dev]

permissions:
  contents: read
  pull-requests: read
  issues: read

jobs:
  pr-validation:
    uses: ./.github/workflows/reusable-pr-validation.yml
