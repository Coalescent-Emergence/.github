This folder contains workflow definitions and notes that were previously at the repository root under `workflows/`.

Note: GitHub Actions reads `.github/workflows/` — this `repo-meta/workflows/` folder is for archival and organizational use.

If you maintain scripts or agent prompts that referenced `workflows/`, update them to `repo-meta/workflows/`.
