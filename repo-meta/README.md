This repository uses `repo-meta/` for non-root governance and archival material moved out of the repository root.

What moved here
- `repo-meta/workflows/` — organizational workflow notes and offline workflow definitions (not `.github/workflows`).

Why
- Keeps the repository root focused on the `.github/` folder and agent sources while preserving discoverability for governance artifacts.

If you previously referenced `workflows/` at the repository root, update links to `org-dot-github/repo-meta/workflows/`.
