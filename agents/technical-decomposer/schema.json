{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Coalescent-Emergence/.github/agents/technical-decomposer/schema.json",
  "title": "Technical Decomposer Output Schema",
  "type": "object",
  "required": ["tasks", "tests", "confidence", "rationale", "handoff_next"],
  "properties": {
    "tasks": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "title", "description", "depends_on", "estimate"],
        "properties": {
          "id": { "type": "string", "pattern": "^T\\d+$" },
          "title": { "type": "string" },
          "description": { "type": "string" },
          "depends_on": {
            "type": "array",
            "items": { "type": "string" }
          },
          "estimate": { "type": "string" },
          "files_affected": {
            "type": "array",
            "items": { "type": "string" }
          }
        }
      }
    },
    "tests": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["type", "description"],
        "properties": {
          "type": {
            "type": "string",
            "enum": ["unit", "integration", "e2e"]
          },
          "description": { "type": "string" },
          "files": {
            "type": "array",
            "items": { "type": "string" }
          },
          "coverage_target": {
            "type": "integer",
            "minimum": 0,
            "maximum": 100
          }
        }
      }
    },
    "ci_considerations": {
      "type": "array",
      "items": { "type": "string" }
    },
    "confidence": {
      "type": "integer",
      "minimum": 0,
      "maximum": 100
    },
    "rationale": { "type": "string" },
    "handoff_next": {
      "$ref": "../_shared/project-handoff-schema.json#/$defs/HandoffNext"
    }
  }
}
