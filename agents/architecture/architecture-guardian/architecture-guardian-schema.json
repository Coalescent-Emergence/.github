{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Coalescent-Emergence/.github/agents/architecture-guardian/architecture-guardian-schema.json",
  "title": "Architecture Guardian Output Schema",
  "type": "object",
  "required": ["violations", "confidence", "rationale", "handoff_next"],
  "properties": {
    "violations": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["adr_id", "severity", "description"],
        "properties": {
          "adr_id": { "type": "string" },
          "severity": {
            "type": "string",
            "enum": ["advisory", "warning", "critical"]
          },
          "description": { "type": "string" },
          "suggestion": { "type": "string" },
          "line_numbers": {
            "type": "array",
            "items": { "type": "string" }
          }
        }
      }
    },
    "compliant_decisions": {
      "type": "array",
      "items": { "type": "string" }
    },
    "missing_adr": {
      "type": "array",
      "items": { "type": "string" }
    },
    "confidence": {
      "type": "integer",
      "minimum": 0,
      "maximum": 100
    },
    "rationale": { "type": "string" },
    "handoff_next": {
      "$ref": "../../_shared/project-handoff-schema.json#/$defs/HandoffNext"
    }
  }
}
